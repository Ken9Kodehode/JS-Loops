//Counting words in text
let text = "Trolls if sometimes as associated with and particular landmarks in Scandinavian folklore, which at times may be explained as formed from a troll exposed to sunlight. Trolls are depicted in a variety of media in modern popular culture."

//removing if + 5 chars on each side

      //instanceIF = text.indexOf("if")
      //if (instanceIF -5 < 0)
      //{
      //    startIF = 0
      //}
      //else
      //{
      //    startIF = instanceIF -5
      //}
      //endIF = instanceIF +5
      //newIfText = text.slice(startIF, endIF)

      //console.log(newIfText);

//removing as +  5 characters on each side
      //instanceAS = text.indexOf("as")
      //startAS = instanceAS -5
      //endAS = instanceAS + 5
      //newASText = text.slice(startAS, endAS)

      //console.log(newASText);

//removing and + 5 characters on each side
      //instanceAND = text.indexOf("and")
      //startAND = instanceAND -5
      //endAND = instanceAND + 5
      //newANDText = text.slice(startAND, endAND)

      //console.log(newANDText);

//replacing if + 5 characters on each side
      //console.log("Replacing if: ");
      //console.log(text.replace("if", "HVIS"));

      //console.log("Replacing as: ");
      //console.log(text.replace("as", "SOM"));

      //console.log("Replacing and: ");
      //console.log(text.replace("and", "OG"));